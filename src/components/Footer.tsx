export default function Footer() {
    return (
        <footer className="text-grey text-sm my-12 font-mono">
            Tehran, Iran — remote · © {new Date().getFullYear()}
        </footer>
    )
}