export default function Header() {
    return (
        <header className="uppercase text-sm sm:text-base flex items-center justify-between font-medium font-mono">
            <span>ZAHRA HANIFI</span>

            <span className="text-primary">AVAILABLE — REMOTE</span>
        </header>
    )
}