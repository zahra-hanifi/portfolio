export default function Header() {
    return (
        <header className="uppercase text-sm sm:text-base flex items-center justify-between font-bold">
            <span>ZAHRA HANIFI</span>

            <span className="text-primary">AVAILABLE — REMOTE</span>
        </header>
    )
}